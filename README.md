# KTOR / KOIN / EXPOSED / ARROW sample webapp

### to build :
- `./gradlew buildFatJar`

### to run
- `docker-compose up -d`

## the remainder to make

- review
- improve test coverage and security
- load test benchmark
- add open-api UI

